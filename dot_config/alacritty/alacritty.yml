env:
  TERM: alacritty

window:
  opacity: 0.95

shell:
  program: /usr/bin/zsh
  args:
    - -l
    - -c
    - 'tmux a -t default || tmux new -s default'

font:
  size: 12
  normal:
    family: HackGen Console NF
  bold:
    family: HackGen Console NF
  italic:
    family: HackGen Console NF

